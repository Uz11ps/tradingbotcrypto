# CUR - Журнал системных изменений

---
## [2026-03-18 19:05:00] Реализовано: PR5/B3 hardening (shard fail-open + shorter slot TTL)
**Почему сделано:**
- В проде периодически повторялся сценарий `index=-1` на всех воркерах после recreate/scale:
  - `RSI worker skipping scan: no shard slot acquired`.
- Корень: stale slot-lease + длинный `worker_shard_slot_lock_ttl_seconds=600`, из-за чего cluster мог до 10 минут простаивать без скана.

**Что изменено:**
- `app/core/config.py`:
  - `worker_shard_slot_lock_ttl_seconds`: `600 -> 120`.
  - добавлены флаги:
    - `worker_shard_fail_open_enabled` (default `True`);
    - `worker_shard_fail_open_after_retries` (default `20`).
- `app/workers/mock_signal_worker.py`:
  - добавлен `no_slot_streak` счетчик;
  - если слот не получен длительно (`>= fail_open_after_retries`), воркер включает fail-open:
    - назначает fallback shard через `_derive_shard_index_from_identity(...)`;
    - продолжает скан вместо полного standby;
  - при успешном захвате Redis slot воркер выходит из fail-open и пишет recovery лог.
  - `runtime_state` расширен полями:
    - `shard_fail_open`
    - `no_slot_streak`.

**Что не менялось:**
- Threshold/fallback логика сигналов (PR5a/PR5b).
- API hotfix по varchar-лимитам.
- Формат карточек в Telegram.

**Ожидаемый runtime-эффект:**
- Даже при временно залипших lease-ключах воркеры не зависают все в `index=-1`.
- После освобождения slot-lease происходит автоматическое восстановление в нормальный shard mode.

**Проверки (локально):**
- `$env:PYTHONPATH='.'; pytest -q` -> `33 passed`.
- `ReadLints` -> ошибок нет.

**Rollback-план:**
- Вернуть `worker_shard_slot_lock_ttl_seconds=600`.
- Отключить fail-open (`WORKER_SHARD_FAIL_OPEN_ENABLED=false`) и убрать изменения worker loop.

---
## [2026-03-18 18:45:00] Операционная валидация: PR5/B3 gate PASS (scale=5 shard distribution restored)
**Что выполнено:**
- Прод-конфиг шардирования возвращен в целевой режим:
  - `WORKER_SHARD_INDEX=-1`
  - `WORKER_SHARD_COUNT=5`
  - slot-ключи `signal:worker_shard_slot:0..4` очищены перед recreate.
- Выполнен `docker compose up -d --scale signal_worker=5 --force-recreate signal_worker`.

**Лог-доказательства:**
- `RSI worker shard setup` показал уникальные индексы `0..4`:
  - `signal_worker-18 -> index=0`
  - `signal_worker-20 -> index=1`
  - `signal_worker-19 -> index=2`
  - `signal_worker-17 -> index=3`
  - `signal_worker-16 -> index=4`
- `RSI shard X: N symbols to scan`:
  - `shard 0: 14 symbols`
  - `shard 1..4: 13 symbols`
  - итог: распределение universe между 5 воркерами корректное, full-duplicate скан не наблюдается.

**Операционный вывод:**
- PR5/B3 gate (runtime) пройден: масштаб `5` работает как shard-mode, а не как 5 копий `shard 0`.
- Текущий next-step по roadmap: PR6/B2 (stage-gate рост universe `500 -> 700 -> 900`) под наблюдением latency/error/signal-noise.

---
## [2026-03-18 14:58:00] Операционная валидация утром: стабильность safe-mode + сравнение Telegram export (день 1 vs день 2)
**Что проверено в проде:**
- Safe-mode (`WORKER_SHARD_COUNT=1`, `WORKER_SHARD_INDEX=0`) стабилен:
  - `RSI shard 0: 56..63 symbols to scan`;
  - есть `decision_trace signal_sent` и `decision_trace reject_user_min_move`;
  - API по `/signals` и `/telemetry/scan-logs` отвечает `200 OK` без `500`/`StringDataRightTruncationError`.

**Сравнение экспортов Telegram (`2026-03-17` vs `2026-03-18`):**
- Источники:
  - `C:\Users\t6are\Downloads\Telegram Desktop\ChatExport_2026-03-17\messages.html`
  - `C:\Users\t6are\Downloads\Telegram Desktop\ChatExport_2026-03-18\messages.html`
- Сигналы формата `Движение:`:
  - день 1: `287`
  - день 2: `188` (снижение шумовой нагрузки примерно на `34.5%`).
- Подпороговые карточки `Движение: +/-4.x%`:
  - день 1: `88`
  - день 2: `0`
  - вывод: фикс threshold-binding реально сработал (исчезли сигналы ниже 5%).
- Strategy-карточки (`Сигнал: Лонг/Шорт` от Pin Bar):
  - день 1: `0`
  - день 2: `58` (новый поток, отдельно наблюдать и валидировать).

**Ключевой риск, выявленный утром:**
- При ручном `--scale signal_worker=5` в текущем safe-config все воркеры стартуют как `index=0 count=1` и сканируют весь universe параллельно.
- Это не целевое shard-распределение; повышает риск дублей и лишней нагрузки.

**Операционный вывод:**
- До фикса shard-resilience (PR5/B3) держим прод в safe-mode `scale=1`.
- Переход на `scale=5` только после восстановления корректного slot-based shard assignment (`0..4` без массового `skipping scan` и без full-duplicate скана).

---
## [2026-03-17 23:58:00] Реализовано: PR5d-hotfix (API 500 на /signals и /telemetry/scan-logs из-за VARCHAR лимитов)
**Что произошло (prod):**
- `POST /telemetry/scan-logs` падал с `StringDataRightTruncationError: value too long for type character varying(32)`.
- `POST /signals` периодически падал `500` для strategy-сигналов, т.к. `signal_type` длиннее `VARCHAR(16)`.

**Корневая причина:**
- Новые значения после live/strategy refactor стали длиннее исторических ограничений БД:
  - `scan_logs.event` (`VARCHAR(32)`) не вмещал `futures_excluded_adapter_disabled`.
  - `signals.signal_type` (`VARCHAR(16)`) не вмещал `post_pump_pullback_short` / `post_dump_bounce_long`.

**Что сделано:**
- `app/db/models.py`:
  - `signals.signal_type` расширен до `String(32)`.
  - `scan_logs.event` расширен до `String(64)`.
- `app/db/init.py`:
  - добавлены idempotent schema-upgrade команды:
    - `ALTER TABLE signals ALTER COLUMN signal_type TYPE VARCHAR(32)`;
    - `ALTER TABLE scan_logs ALTER COLUMN event TYPE VARCHAR(64)`.
- `app/api/router.py`:
  - добавлен safety-normalizer `_fit_varchar(...)`;
  - в `/signals` и `/telemetry/scan-logs` значения приводятся к лимитам колонок перед записью, чтобы исключить runtime 500 при будущих длинных значениях.

**Что не менялось:**
- Логика детекции pump/dump и TF fallback.
- Shard lease модель и распределение universe.

**Проверки (локально):**
- `$env:PYTHONPATH='.'; pytest -q` -> `33 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Runtime-критерий успеха после деплоя:**
- `POST /signals` -> `HTTP 200`.
- `POST /telemetry/scan-logs` -> `HTTP 200`.
- В API логах отсутствует `StringDataRightTruncationError`.

**Rollback-план:**
- Откатить изменения в `app/db/models.py`, `app/db/init.py`, `app/api/router.py` до предыдущего коммита.

---
## [2026-03-17 23:38:00] Реализовано: PR5b-hotfix (NameError в strategy ветке после TF refactor)
**Что произошло (prod):**
- В логах зафиксирован крэш воркера после `Failed to save/send RSI feed alert ...`:
  - `NameError: name 'timeframe' is not defined` в `app/workers/mock_signal_worker.py` внутри strategy-блока.
- Ошибка проявилась после PR5b (refactor selected/evaluation timeframe), где в части strategy осталась старая переменная `timeframe`.

**Что сделано:**
- `app/workers/mock_signal_worker.py`:
  - в strategy-блоке заменены остаточные ссылки `timeframe` -> `selected_tf`:
    - `fetch_recent_bars(... timeframe=selected_tf ...)`
    - `detect_pinbar_strategy_signal(... timeframe=selected_tf ...)`
    - debug/reject payload в `_debug_raw_candidate` теперь также использует `selected_tf`.

**Что не менялось:**
- Алгоритм live_spike fallback (`_resolve_evaluation_window`) и decision semantics.
- Логика dedup/cooldown и shard lease.
- Формат Telegram карточки.

**Риски:**
- Низкие: точечный fix переменной без изменения бизнес-логики.

**Проверки (локально):**
- `$env:PYTHONPATH='.'; pytest -q tests/test_worker_timeframe_fallback.py` -> `3 passed`.
- `$env:PYTHONPATH='.'; pytest -q` -> `33 passed`.

**Что важно по логам:**
- `TelegramForbiddenError: bot was blocked by the user` — это ожидаемое поведение для конкретного `chat_id` и не является причиной падения воркера.
- Критическая причина рестартов — именно `NameError`, устранена этим hotfix.

**Rollback-план:**
- Откатить изменение в `app/workers/mock_signal_worker.py` до предыдущего коммита PR5b.

---
## [2026-03-17 22:04:00] Планируемые правки: PR5a (trust/correctness: settings+TF observability)
**1) Список изменяемых файлов:**
- `CUR`
- `app/services/user_settings.py`
- `app/workers/mock_signal_worker.py`
- `tests/test_user_settings.py` (новый)

**2) Точная цель:**
- Устранить рассинхрон пользовательского порога и реального decision threshold:
  - для текущей модели применять `min_price_move_pct` как единый базовый trigger порог для 5m/15m feed decision;
  - убрать скрытый 15m default, который может обходить пользовательскую настройку.
- Добавить обязательную observability-цепочку по настройкам/ТФ прямо в worker-логах:
  - `selected_tf`, `effective_tf`, `selected_threshold`, `effective_threshold`, `trigger_mode`, `chat_id`.

**3) Что не меняется:**
- Fallback-логика timeframe (формализация отдельно в PR5b).
- Cache/state scaling, sharding, universe growth.
- UI формат сигнала (`close` removal отдельно в PR5c).

**4) Риски:**
- Возможно снижение количества сигналов при более строгом применении пользовательского порога.
- Риск повышенного лог-шума (ограничивается логом один раз на chat/cycle).

**5) Тесты:**
- Новый unit-тест на resolution дефолтных settings (`tests/test_user_settings.py`).
- `pytest -q tests/test_user_settings.py`
- `pytest -q`

**6) Rollback-план:**
- Вернуть предыдущую схему threshold resolution в `user_settings.py` и `mock_signal_worker.py`.
- Оставить observability-логи отключаемыми через уровень логирования.

---
## [2026-03-17 22:18:00] Реализовано: PR5a (trust/correctness: settings+TF observability)
**Что сделано:**
- `app/services/user_settings.py`:
  - устранён скрытый 15m-default:
    - `price_change_15m_trigger_pct` выровнен с `min_price_move_pct` в defaults;
    - в effective settings `price_change_15m_trigger_pct` теперь вычисляется из реального `min_price_move_pct` пользователя.
- `app/workers/mock_signal_worker.py`:
  - добавлен `settings_trace` лог (для каждого chat/cycle):
    - `selected_tf`, `effective_tf`, `selected_threshold`, `effective_threshold`, `trigger_mode`.
  - добавлен `decision_trace` лог:
    - `reject_user_min_move` с `change` и `threshold`;
    - `signal_sent` с `change`, `threshold`, `trigger_source`.
  - усилен threshold binding в feed-eval:
    - `trigger_5m = selected_threshold`;
    - `trigger_15m` не может быть ниже `selected_threshold`.
- добавлен unit-тест `tests/test_user_settings.py` на выравнивание default-порогов.

**Что не менялось:**
- TF fallback semantics (отдельно PR5b).
- live-only формат Telegram карточки (отдельно PR5c).
- cache/state scaling и universe rollout.

**Проверки:**
- `$env:PYTHONPATH='.'; pytest -q tests/test_user_settings.py` -> `1 passed`.
- `$env:PYTHONPATH='.'; pytest -q` -> `30 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Команды runtime-подтверждения (после деплоя) и что ищем:**
- `docker compose logs --since=15m signal_worker | grep "settings_trace"`
  - ищем совпадение `selected_threshold` и `effective_threshold`, корректный `effective_tf`.
- `docker compose logs --since=15m signal_worker | grep "decision_trace reject_user_min_move"`
  - ищем отказы ниже порога с корректным `change < threshold`.
- `docker compose logs --since=15m signal_worker | grep "decision_trace signal_sent"`
  - проверяем, что отправленные сигналы не нарушают `change >= threshold`.

**Rollback-план:**
- Откат изменений в `user_settings.py` и `mock_signal_worker.py`.
- Возврат к предыдущей модели порогов до PR5a.

---
## [2026-03-17 23:05:00] Операционные наблюдения: PR5a deployment validation (prod)
**Что подтверждено логами:**
- после деплоя и очистки stale shard-slot ключей:
  - `RSI shard 0..4` активны, скан идет стабильно;
  - `settings_trace` присутствует в рабочих циклах на активных воркерах.
- по `settings_trace` подтверждено совпадение выбранных и effective параметров:
  - `selected_threshold=5.0000`
  - `effective_threshold=5.0000`
  - `selected_tf` / `effective_tf` согласованы.

**Что выявлено дополнительно:**
- при scale > `WORKER_SHARD_COUNT` часть воркеров закономерно уходит в standby (`worker_shard_index=-1`).
- повторно подтверждено: stale Redis slot lease может временно блокировать reacquire после recreate (лечится controlled cleanup slot keys).
- за окно проверки не зафиксировано `decision_trace reject_user_min_move` (не обязательно дефект; зависит от потока кандидатов), при этом enforcement флаг в `.env` явно включен: `SIGNAL_DISABLE_DOUBLE_MIN_MOVE_FILTER=false`.
- есть отдельная runtime-проблема API telemetry:
  - `POST /telemetry/scan-logs` -> `HTTP 500` (вне scope PR5a, выносится отдельным hotfix-пунктом).

---
## [2026-03-17 23:10:00] Планируемые правки: PR5b (timeframe fallback formalization + explicit logs)
**1) Список изменяемых файлов:**
- `CUR`
- `app/workers/mock_signal_worker.py`
- `tests/test_worker_timeframe_fallback.py` (новый)

**2) Точная цель:**
- Явно формализовать fallback-поведение timeframe для live_spike/both режимов:
  - если выбран `1h` или `4h`, live trigger-window явно фиксируется как 15m-window (900s) с пометкой fallback;
  - убрать скрытое поведение без объяснения в логах.
- Добавить обязательные decision/log поля:
  - `selected_tf`, `evaluation_tf`, `window_seconds`, `fallback_applied`, `fallback_reason`.

**3) Что не меняется:**
- UI карточка сигнала (live-only формат отдельно в PR5c).
- Cache/state scaling и universe rollout.
- dedup/anti-flip логика.

**4) Риски:**
- Рост лог-объема из-за fallback trace (смягчается краткими структурными полями).

**5) Тесты:**
- Unit-тесты на resolution fallback mapping.
- `pytest -q tests/test_worker_timeframe_fallback.py`
- `pytest -q`

**6) Rollback-план:**
- Вернуть предыдущую схему trigger-window resolution в worker.
- Удалить fallback trace поля из decision payload/logs.

---
## [2026-03-17 23:22:00] Реализовано: PR5b (timeframe fallback formalization + explicit logs)
**Что сделано:**
- `app/workers/mock_signal_worker.py`:
  - добавлен явный resolver `_resolve_evaluation_window(...)`:
    - `live_spike/both`:
      - `5m` -> `evaluation_tf=5m`, `window_seconds=300`;
      - `15m` -> `evaluation_tf=15m`, `window_seconds=900`;
      - `1h/4h` -> fallback `evaluation_tf=15m`, `window_seconds=900`, `fallback_reason=live_trigger_window_fallback_to_15m`.
    - `candle`:
      - `5m/15m/1h/4h` остаются без fallback.
  - добавлен `tf_fallback_trace` лог:
    - `selected_tf`, `evaluation_tf`, `window_seconds`, `fallback_reason`, `trigger_mode`.
  - в decision/raw telemetry payload добавлены:
    - `selected_tf`, `effective_tf`, `window_seconds`, `fallback_applied`, `fallback_reason`.
  - исправлена проверка `reject_user_min_move`:
    - threshold выбирается по `evaluation_tf` (`5m` vs non-`5m`), а не жестко по `trigger_5m`.
  - `decision_trace signal_sent` расширен полями `eval_tf/window_seconds/fallback`.
- добавлен unit-test `tests/test_worker_timeframe_fallback.py` (3 кейса fallback mapping).

**Что не менялось:**
- UI карточка сигнала (live-only формат отдельно PR5c).
- cache/state scaling, worker scaling и universe rollout.
- dedup/anti-flip.

**Проверки:**
- `$env:PYTHONPATH='.'; pytest -q tests/test_worker_timeframe_fallback.py` -> `3 passed`.
- `$env:PYTHONPATH='.'; pytest -q` -> `33 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Команды runtime-подтверждения (после деплоя) и что ищем:**
- `docker compose logs --since=15m signal_worker | grep "tf_fallback_trace"`
  - ищем явные fallback события для `selected_tf=1h/4h` в `live_spike/both`.
- `docker compose logs --since=15m signal_worker | grep "decision_trace signal_sent"`
  - подтверждаем присутствие `eval_tf/window_seconds/fallback`.
- `docker compose logs --since=15m signal_worker | grep "decision_trace reject_user_min_move"`
  - проверяем, что reject threshold рассчитывается по `evaluation_tf`.

**Rollback-план:**
- Вернуть предыдущую схему evaluation window без explicit fallback.
- Убрать fallback trace поля из decision/raw payload.

---
## [2026-03-16 21:22:00] Планируемые правки: PR-пакет 4c (strict unique shard assignment, без detector changes)
**1) Список изменяемых файлов:**
- `CUR`
- `app/workers/mock_signal_worker.py`
- `app/core/config.py`

**2) Точная цель:**
- Принудительно исключить дубли shard-сканирования:
  - если worker не захватил уникальный shard slot через Redis, он не сканирует рынок;
  - scanning разрешается только при валидном slot `0..N-1`.
- Сохранить текущую owner-lease модель shadow ingest без изменения detector.

**3) Что не меняется:**
- Detector/trigger логика.
- API/DB schema.
- Reconnect/backoff/jitter из PR4b.

**4) Риски:**
- При проблемах Redis часть воркеров может временно не сканировать (предпочтительно дублированию).

**5) Тесты:**
- `pytest -q`
- runtime-проверка:
  - `runtime_state` без дублирующих shard index среди активных сканеров;
  - отсутствие параллельного скана одинаковых символов разными shard.

**6) Rollback-план:**
- Вернуть предыдущий commit PR4b.
- В аварийном режиме выставить фиксированный `WORKER_SHARD_INDEX` для single-worker fallback.

---
## [2026-03-16 21:30:00] Реализовано: PR-пакет 4c (strict unique shard assignment, без detector changes)
**Что сделано:**
- `app/workers/mock_signal_worker.py`:
  - включен strict shard mode при `WORKER_SHARD_INDEX < 0` и доступном Redis:
    - если slot не получен, `shard_index=-1` и scan пропускается;
    - scan запускается только при валидном shard slot `0..N-1`;
  - добавлен явный warning-log при пропуске скана из-за отсутствия shard slot;
  - добавлен retry interval между повторными попытками получить shard slot.
- `app/core/config.py`:
  - добавлен `worker_shard_slot_retry_interval_seconds`.

**Что не менялось:**
- Detector/trigger логика.
- API/DB schema.
- Reconnect/backoff/jitter и owner lease логика из PR4b/PR4a.

**Проверки:**
- `pytest -q` -> `29 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Откат на PR4b commit.
- При необходимости временно задать фиксированный `WORKER_SHARD_INDEX` для аварийного режима.

---
## [2026-03-16 20:55:00] Планируемые правки: PR-пакет 4b (reconnect/backoff/jitter + counters/health)
**1) Список изменяемых файлов:**
- `CUR`
- `app/services/live_ingest.py`
- `app/core/config.py`
- `app/workers/mock_signal_worker.py`
- `tests/test_live_ingest.py`

**2) Точная цель:**
- Добавить контролируемый reconnect c exponential backoff + jitter для shadow ingest.
- Расширить counters/health для быстрого runtime-аудита:
  - attempts/disconnects/timeouts/errors
  - decode_failures/pongs_sent/subscriptions_sent
  - cache_age_ms
- Оставить detector path без изменений.

**3) Что не меняется:**
- Логика detection/trigger.
- API/DB schema.
- Shard assignment/owner lease semantics (исправлены в PR4a/hotfix).

**4) Риски:**
- Неверная формула backoff может дать слишком длинные паузы reconnect.
- Избыточное логирование может увеличить шум.

**5) Тесты:**
- Unit-тесты на backoff/jitter границы.
- Unit-тесты на расширенные ingest counters (decode/pong).
- `pytest -q tests/test_live_ingest.py`
- `pytest -q`

**6) Rollback-план:**
- Вернуть предыдущий ingest модуль (до PR4b).
- Оставить PR4a shard+owner фиксы активными.

---
## [2026-03-16 21:06:00] Реализовано: PR-пакет 4b (reconnect/backoff/jitter + counters/health)
**Что сделано:**
- `app/services/live_ingest.py`:
  - добавлен exponential reconnect backoff с cap и jitter;
  - расширены counters:
    - `attempts_total`
    - `disconnects_total`
    - `timeouts_total`
    - `decode_failures_total`
    - `pongs_sent_total`
    - `subscriptions_sent_total`
    - `cache_age_ms`
  - добавлен helper `_next_reconnect_delay(...)` для контролируемой задержки reconnect.
- `app/core/config.py`:
  - добавлены параметры:
    - `signal_live_ws_reconnect_max_seconds`
    - `signal_live_ws_reconnect_jitter_seconds`
- `app/workers/mock_signal_worker.py`:
  - `LiveShadowIngestor` инициализируется с max/jitter параметрами;
  - лог `Shadow ingest stats` расширен новыми health counters для runtime аудита.
- `tests/test_live_ingest.py`:
  - добавлен тест на границы backoff/jitter delay.

**Что не менялось:**
- Detector/trigger логика.
- API/DB schema.
- Shard/owner lease модель из PR4a/hotfix.

**Проверки:**
- `pytest -q tests/test_live_ingest.py` -> `5 passed`.
- `pytest -q` -> `29 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Откат PR4b commit.
- Сохранить PR4a/hotfix как стабильный baseline.

---
## [2026-03-16 20:32:00] Планируемые правки: PR-пакет 4a-hotfix (shard uniqueness + runtime_state truth)
**1) Список изменяемых файлов:**
- `CUR`
- `app/workers/mock_signal_worker.py`
- `app/core/config.py`
- `tests/test_worker_sharding.py`

**2) Точная цель:**
- Убрать коллизии `worker_shard_index` между воркерами (детерминированный уникальный shard slot через Redis lock).
- Сделать `runtime_state` правдивым:
  - `shadow_ingest_owner` отражает реального владельца;
  - `ingest_owner_acquired` выставляется после фактического acquire;
  - `ingest_owner_lost` выставляется после фактической потери lease;
  - `assigned_symbols_count` отражает реальный размер shard assignment, а не placeholder.

**3) Что не меняется:**
- Reconnect/backoff/jitter (PR4b).
- Telemetry schema/API/DB.
- Основная логика отправки сигналов.

**4) Риски:**
- Ошибка в shard slot lease может привести к временному перекосу распределения.
- Изменение shard index на лету (при lease-loss) может краткосрочно изменить набор символов у воркера.

**5) Тесты:**
- `pytest -q tests/test_worker_sharding.py`
- `pytest -q`

**6) Rollback-план:**
- Вернуть предыдущий PR4a commit.
- Временно задать фиксированный `WORKER_SHARD_INDEX` для одиночного worker режима (если нужен аварийный обход).

---
## [2026-03-16 20:41:00] Реализовано: PR-пакет 4a-hotfix (shard uniqueness + runtime_state truth)
**Что сделано:**
- `app/workers/mock_signal_worker.py`:
  - добавлен Redis shard slot guard (`signal:worker_shard_slot:<idx>`) для уникального назначения shard index;
  - shard slot обновляется/продлевается в runtime loop;
  - release shard slot при graceful shutdown;
  - `runtime_state` теперь логирует правдивые значения:
    - `worker_shard_index`
    - `worker_shard_total`
    - `assigned_symbols_count` (из фактического shard assignment)
    - `shadow_ingest_owner`
    - `ingest_owner_acquired`
    - `ingest_owner_lost`
  - `ingest_owner_acquired` и `shadow_ingest_owner` синхронизированы с реальным состоянием owner lease.
- `app/core/config.py`:
  - добавлен `worker_shard_slot_lock_ttl_seconds` для управления TTL shard slot lease.
- `tests/test_worker_sharding.py`:
  - сохранены и подтверждены тесты sharding fallback.

**Что не менялось:**
- Reconnect/backoff/jitter (оставлено на PR4b).
- Telemetry schema/API/DB.
- Основной прод-путь отправки сигналов.

**Проверки:**
- `pytest -q tests/test_worker_sharding.py` -> `3 passed`.
- `pytest -q` -> `28 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Откат на предыдущий commit PR4a.
- При необходимости временный single-worker режим для аварийной стабилизации.

---
## [2026-03-16 20:05:00] Планируемые правки: PR-пакет 4a (Shard fix + single-owner guard + runtime логирование)
**1) Список изменяемых файлов:**
- `CUR`
- `app/workers/mock_signal_worker.py`
- `app/core/config.py`
- `tests/test_worker_sharding.py`

**2) Точная цель:**
- Исправить auto-shard распределение, чтобы воркеры не падали в `index=0` при одинаковом fallback.
- Добавить single-owner guard для shadow ingest (один владелец через Redis lease).
- Добавить runtime-логи для быстрой проверки:
  - `worker_shard_index`
  - `worker_shard_total`
  - `assigned_symbols_count`
  - `shadow_ingest_owner`
  - `ingest_owner_acquired`
  - `ingest_owner_lost`

**3) Что не меняется:**
- Основной прод-путь генерации/отправки сигналов.
- Reconnect/backoff/jitter механика WS (это отдельный PR4b).
- Telemetry schema/API/DB контракты.

**4) Риски:**
- Ошибка в owner-lease может выключить shadow ingest.
- Некорректный shard fallback может дать частичные пересечения по символам.

**5) Тесты:**
- `tests/test_worker_sharding.py` на стабильный auto-shard fallback.
- `pytest -q tests/test_worker_sharding.py`
- `pytest -q`

**6) Rollback-план:**
- Отключить shadow ingest (`SIGNAL_SHADOW_MODE_ENABLED=false`).
- Вернуть предыдущий worker commit.
- Проверить восстановление baseline-поведения без owner-guard.

---
## [2026-03-16 20:17:00] Реализовано: PR-пакет 4a (Shard fix + single-owner guard + runtime логирование)
**Что сделано:**
- `app/workers/mock_signal_worker.py`:
  - добавлен стабильный fallback для auto-shard:
    - если `HOSTNAME` без суффикса `-N`, индекс считается по детерминированному hash identity;
  - добавлен Redis lease guard для single-owner shadow ingest:
    - lease key: `signal:shadow_ingest_owner`;
    - acquire/renew/release;
    - при потере lease shadow task останавливается;
  - добавлены runtime-логи с ключами:
    - `worker_shard_index`
    - `worker_shard_total`
    - `assigned_symbols_count`
    - `shadow_ingest_owner`
    - `ingest_owner_acquired`
    - `ingest_owner_lost`
- `app/core/config.py`:
  - добавлен параметр `signal_live_ingest_owner_lock_ttl_seconds`.
- `tests/test_worker_sharding.py`:
  - добавлены тесты стабильного shard hash fallback и разрешения shard index при hostname без `-N`.

**Что не менялось:**
- Механика reconnect/backoff/jitter (перенесено в PR4b).
- Telemetry schema/API/DB контракты.
- Основной прод-путь сигналов.

**Проверки:**
- `pytest -q tests/test_worker_sharding.py` -> `3 passed`.
- `pytest -q` -> `28 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Выключить shadow ingest флагом.
- Вернуть предыдущий commit worker/config.
- Проверить, что baseline path продолжает работать как до PR4a.

---
## [2026-03-16 19:45:00] Runtime-check после PR-пакета 3 + команды деплоя/диагностики
**Короткий итог состояния системы:**
- Внесен фундамент live-first миграции без переключения основного prod-пути:
  - `LiveSignalContract` + timestamp chain;
  - bounded `MarketStateCache` (TTL, caps, eviction);
  - shadow WS ingest (`trade + depth`) в worker.
- Подтверждено на проде:
  - shadow ingest стартует и держит соединение (`connected=True`);
  - cache наполняется (`symbols=3`, `points~1400+`, `updates` растут);
  - ресурсы стабильны (worker ~3-6% CPU, ~31% RAM лимита);
  - критичных ошибок старого signal path не выявлено.
- Обнаруженный нюанс:
  - shadow ingest стартует во всех 5 воркерах (из-за shard autodetect -> `index=0` у всех).
  - Это не ломает прод-путь, но создает лишнюю нагрузку; требуется отдельный микро-фикс.

**Системные команды (факт выполнения):**
- Сервер:
  - `cd /opt/tradingbotcrypto`
  - `git status -sb`
  - `git pull --ff-only origin main`
  - `ls app/services/live_ingest.py`
  - `docker compose --profile worker up -d --build --remove-orphans --scale signal_worker=5`
  - `docker compose logs --tail=200 signal_worker | grep -E "Shadow live ingest started|Shadow ingest stats|live-shadow-ingest"`
  - `docker compose logs --tail=400 signal_worker | grep -i shadow`
  - `docker compose logs --tail=400 signal_worker | grep "RSI worker shard setup"`
  - `grep -E "^WORKER_SHARD_INDEX=|^WORKER_SHARD_COUNT=" .env`
  - `docker compose logs --tail=600 signal_worker | grep -A 25 "Live shadow ingest loop error"`
  - `docker logs --tail=500 tradingbotcrypto-signal_worker-1 2>&1 | grep -A 80 "Live shadow ingest loop error"`
  - `docker compose logs --since=10m signal_worker | grep "Shadow ingest stats"`
  - `docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.MemPerc}}"`
  - `docker compose logs --since=10m signal_worker | grep -E "Failed to save/send RSI feed alert|Signal retention prune failed|RSI reject: bad_data" | tail -n 50`
- Локально (подготовка/публикация изменений):
  - `git status --short`
  - `git diff -- .`
  - `git log -5 --oneline`
  - `git add CUR pyproject.toml app/core/config.py app/workers/mock_signal_worker.py app/services/signal_contract.py app/services/market_state_cache.py app/services/live_ingest.py tests/test_signal_contract.py tests/test_market_state_cache.py tests/test_live_ingest.py`
  - `git commit -m "Add shadow live-ingest foundation with bounded market cache."`
  - `git push origin main`

**Оперативные команды контроля live по всем воркерам:**
- Проверка live ingest heartbeat:
  - `docker compose logs --since=10m signal_worker | grep "Shadow ingest stats"`
- Проверка shard распределения:
  - `docker compose logs --since=10m signal_worker | grep "RSI worker shard setup"`
- Проверка ошибок ingest:
  - `docker compose logs --since=10m signal_worker | grep -A 40 "Live shadow ingest loop error"`
- Проверка нагрузки:
  - `docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.MemPerc}}"`

---
## [2026-03-16 11:05:00] Планируемые правки: PR-пакет 1 (Live contract + feature flags, без смены runtime)
**1) Список изменяемых файлов:**
- `app/core/config.py`
- `CUR`

**Новые файлы:**
- `app/services/signal_contract.py`
- `tests/test_signal_contract.py`

**2) Точная цель:**
- Зафиксировать единый `LiveSignalContract` для будущего live-first pipeline.
- Ввести флаги безопасного внедрения:
  - `signal_live_detector_enabled`
  - `signal_shadow_mode_enabled`
  - `signal_send_enabled`
  - `signal_contract_version`
- Не менять текущий runtime-путь генерации/отправки сигналов.

**3) Что не меняется:**
- Логика `candle/live_spike/both` и текущий worker flow.
- API роуты/схемы и SQL-модели.
- Dedup/cooldown поведение.

**4) Риски:**
- Низкий риск: возможна только регрессия загрузки настроек, если ошибиться в типах/дефолтах.

**5) Тесты:**
- Новый unit-тест контракта (валидация обязательных полей и timestamp chain).
- `pytest -q tests/test_signal_contract.py`
- `pytest -q` (полный прогон)

**6) Rollback-план:**
- Удалить новые флаги и модуль контракта из этого PR.
- Возврат к предыдущему состоянию без влияния на runtime (поведение системы не меняется).

---
## [2026-03-16 11:18:00] Реализовано: PR-пакет 1 (Live contract + feature flags, без смены runtime)
**Что сделано:**
- Добавлены feature flags в `app/core/config.py`:
  - `signal_live_detector_enabled=False`
  - `signal_shadow_mode_enabled=True`
  - `signal_send_enabled=True`
  - `signal_contract_version="v2"`
- Добавлен новый модуль `app/services/signal_contract.py`:
  - `SignalTimestamps` с валидацией timestamp chain;
  - `LiveSignalContract` с обязательными полями:
    - `baseline_price`
    - `current_price`
    - `change_pct`
    - `trigger_source`
    - `window_seconds`
  - метод `to_payload()` для детерминированной сериализации.
- Добавлены unit-тесты `tests/test_signal_contract.py`:
  - позитивный кейс формирования payload;
  - негативный кейс на невалидную timestamp chain.

**Что не менялось:**
- Текущий runtime pipeline и доставка сигналов.
- БД/роуты/API-контракт.
- Dedup/cooldown и candle/live режимы в текущем воркере.

**Проверки:**
- `pytest -q tests/test_signal_contract.py` -> `2 passed`.
- `pytest -q` -> `18 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Удалить `app/services/signal_contract.py` и `tests/test_signal_contract.py`.
- Убрать новые флаги из `app/core/config.py`.
- Runtime поведение останется прежним (этап не подключал новый контракт в pipeline).

---
## [2026-03-16 11:27:00] Планируемые правки: PR-пакет 2 (In-memory live market cache, без подключения в worker)
**1) Список изменяемых файлов:**
- `CUR`

**Новые файлы:**
- `app/services/market_state_cache.py`
- `tests/test_market_state_cache.py`

**2) Точная цель:**
- Добавить bounded in-memory cache для live market state:
  - ограничение по памяти/точкам;
  - TTL-cleanup;
  - гибридный `current_price`: `mid=(bid+ask)/2`, fallback на `last_trade`.
- Подготовить безопасный фундамент для последующего WS ingest.

**3) Что не меняется:**
- Текущий pipeline воркера и отправка сигналов.
- API/БД/схемы.
- Логика dedup/cooldown.

**4) Риски:**
- Некорректная очистка буфера (TTL/eviction) может давать рассинхрон cache state.

**5) Тесты:**
- Unit-тесты:
  - выбор `mid` vs `last_trade`;
  - TTL cleanup;
  - bounded eviction по лимиту точек.
- `pytest -q tests/test_market_state_cache.py`
- `pytest -q`

**6) Rollback-план:**
- Удалить новый модуль cache и его тесты.
- Этап не подключается в runtime, откат без влияния на production flow.

---
## [2026-03-16 11:34:00] Реализовано: PR-пакет 2 (In-memory live market cache, без подключения в worker)
**Что сделано:**
- Добавлен `app/services/market_state_cache.py`:
  - `MarketPricePoint` (last/bid/ask, `mid_price`, `current_price`, `price_source`);
  - `MarketStateCache` с ограничениями:
    - `ttl_seconds`
    - `max_points_per_symbol`
    - `max_symbols`
  - методы:
    - `upsert(...)`
    - `get_latest(...)`
    - `get_baseline_price(...)` (first-in-window)
    - `cleanup(...)`
  - политика цены: `live_mid` при наличии BBO, иначе `live_trade`.
- Добавлены unit-тесты `tests/test_market_state_cache.py`:
  - приоритет `mid` над `last_trade`;
  - fallback на `last_trade`;
  - TTL cleanup;
  - eviction по лимиту символов.

**Что не менялось:**
- Worker runtime flow и API/БД.
- Текущая отправка сигналов и dedup/cooldown pipeline.

**Проверки:**
- `pytest -q tests/test_market_state_cache.py` -> `4 passed`.
- `pytest -q` -> `22 passed`.
- `ReadLints` -> ошибок нет.

**Rollback-план:**
- Удалить `app/services/market_state_cache.py` и `tests/test_market_state_cache.py`.
- Runtime поведение не затронуто, откат безопасный.

---
## [2026-03-16 11:48:00] Планируемые правки: PR-пакет 3 (Shadow WS ingest внутри worker)
**1) Список изменяемых файлов:**
- `CUR`
- `app/workers/mock_signal_worker.py`
- `pyproject.toml`

**Новые файлы:**
- `app/services/live_ingest.py`
- `tests/test_live_ingest.py`

**2) Точная цель:**
- Добавить shadow WS ingestion task внутри текущего worker (без switch signal path).
- Наполнять `MarketStateCache` live-данными (`trade + best bid/ask`, hybrid fallback).
- Логировать health/stat counters:
  - reconnects
  - messages
  - updates
  - cache symbols/points

**3) Что не меняется:**
- Старый production signal pipeline (candle/live_spike/both) остаётся основным.
- Отправка сигналов и API/БД контракт не переключаются на новый live detector.

**4) Риски:**
- Нестабильность WS соединения (reconnect storm).
- Шум в логах при неверном формате payload.
- Потенциальный рост CPU/RAM (контролируется bounded cache и shadow-only режимом).

**5) Тесты:**
- Unit-тесты парсинга payload и обновления cache.
- `pytest -q tests/test_live_ingest.py`
- `pytest -q`

**6) Rollback-план:**
- Немедленно отключить shadow ingest через флаг.
- При необходимости удалить модуль `live_ingest` и интеграцию в worker.
- Старый production path останется неизменным.

---
## [2026-03-16 12:02:00] Реализовано: PR-пакет 3 (Shadow WS ingest внутри worker)
**Что сделано:**
- Добавлена зависимость `httpx-ws` в `pyproject.toml`.
- Добавлен модуль `app/services/live_ingest.py`:
  - `LiveShadowIngestor` (WS connect/reconnect loop);
  - subscribe на `trade + depth5` каналы;
  - декодирование `TextMessage/BytesMessage` (включая gzip payload);
  - auto pong для ping-сообщений;
  - обновление `MarketStateCache` с hybrid fallback (`live_mid` -> `live_trade`);
  - runtime counters: `connected/reconnects/messages/updates/errors`.
- Обновлен `app/services/market_state_cache.py`:
  - добавлены `symbols_count()` и `points_count()` для health мониторинга.
- Интеграция в `app/workers/mock_signal_worker.py`:
  - shadow ingest стартует как background task внутри worker;
  - запуск только при `signal_shadow_mode_enabled` и только на shard `0`;
  - периодический health-log ingest/cache статистики;
  - аккуратная остановка task в `finally`.
- Расширен `app/core/config.py`:
  - `signal_live_ws_url`
  - `signal_live_shadow_symbols`
  - `signal_live_ws_reconnect_seconds`
  - `signal_live_shadow_log_interval_cycles`
- Добавлены тесты `tests/test_live_ingest.py`:
  - text/gzip decode;
  - payload apply в cache;
  - ping/pong extraction.

**Что не менялось:**
- Старый production signal path (детекция/отправка) не переключался на новый live detector.
- API/БД контракт сигналов без hard-cut.

**Проверки:**
- `pytest -q tests/test_live_ingest.py` -> `4 passed`.
- `pytest -q` -> `26 passed`.
- `ReadLints` по измененным файлам -> ошибок нет.

**Rollback-план:**
- Отключить `signal_shadow_mode_enabled=false`.
- При необходимости удалить `live_ingest` integration из worker и новый модуль.
- Прод-путь сигналов останется рабочим и прежним.

---
## [2026-03-12 19:20:00] Реализовано: closed-candle price + упрощённый UI + постоянный нижний inline-блок
**Что сделано:**
- Исправлен источник цены в snapshot:
  - в `build_snapshot` используется последняя закрытая свеча, а не формирующийся бар;
  - расчёты `current_close`, `prev_close`, `price_change_*` переведены на закрытые бары;
  - в snapshot добавлены метки времени сигнальной свечи:
    - `signal_candle_open_time_ms`
    - `signal_candle_close_time_ms`.
- Упрощена лента сигналов:
  - в карточке feed-сигнала убрана строка `Рекомендация: Лонг/Шорт`;
  - направление оставлено только в стратегических сигналах.
- Пересобран Telegram UI:
  - добавлен постоянный нижний inline-блок на экранах:
    - `Текущие настройки`
    - `Настройки`
    - `Главное меню`;
  - добавлен callback `menu:status` с pop-up по текущей конфигурации;
  - упрощены подписи пунктов настроек.
- Добавлены явные подтверждения изменения параметров:
  - триггер цены,
  - тип рынка,
  - режимы сигналов,
  - сброс/отмена.
- Увеличен базовый target для worker pool:
  - дефолтное значение реплик поднято до `5`;
  - дефолт `WORKER_SHARD_COUNT` в compose обновлён до `5`;
  - deploy script по умолчанию запускает `5` worker-реплик.

**Ожидаемый эффект:**
- Цена в сигнале ближе к рыночной, без эффекта “запаздывающего” незакрытого бара.
- Потоковая лента чище и без лишних торговых рекомендаций.
- Навигация в боте быстрее: статус всегда доступен снизу, вход в настройки в один клик.
- Пользователь сразу видит подтверждение изменения каждой ключевой настройки.

**Проверки:**
- `pytest -q` -> `16 passed`.
- `ReadLints` по изменённым файлам -> ошибок нет.

---
## [2026-03-12 18:40:00] Планируемые правки: UI-панель снизу + цена по закрытой свече + +1 воркер
**Что делаем:**
- Исправляем источник цены для потоковых RSI-сигналов:
  - брать цену из последней закрытой свечи на момент сигнала;
  - исключить текущий формирующийся бар из расчёта `current_close` и краткосрочных изменений.
- Дорабатываем Telegram UI:
  - постоянный нижний inline-блок на экранах с тремя действиями:
    - просмотр текущих настроек,
    - переход в настройки,
    - переход в главное меню;
  - упрощаем экран настроек и тексты управления.
- Добавляем понятные подтверждения изменений:
  - пример: при смене триггера с `5` на `10` выводить уведомление вида
    `Триггер цены обновлён: 10.0%`.
- Разделяем presentation:
  - в ленте `pump/dump` убираем строку рекомендации `Лонг/Шорт`;
  - в стратегических сигналах направление (`Лонг/Шорт`) оставляем.
- Увеличиваем целевой worker pool на `+1`:
  - перейти на 5 воркеров как базовый рабочий режим.

**Что ожидаем:**
- Цена в потоке становится ближе к рынку и не запаздывает из-за незакрытого бара.
- UI проще, быстрее и понятнее в ежедневном управлении.
- Пользователь сразу видит, что настройка реально применена.
- Лента не перегружена торговыми рекомендациями.
- Выше пропускная способность сканирования за счёт дополнительного воркера.

**После реализации:**
- Прогон тестов и базовой runtime-проверки.
- Push в `origin/main`.
- Деплой на сервер и проверка логов `api/bot/signal_worker`.

---
## [2026-03-11 10:15:00] Планируемые правки: глобальный рефакторинг signal engine + market type + retention
**Что будем менять:**
- Пересобираем signal pipeline для двух режимов:
  - `Лента pump/dump`;
  - `Сигналы стратегии` (отклонение + pin bar, контртренд: `short` после pump, `long` после dump).
- Вводим пользовательскую настройку типа рынка (глобально на чат):
  - `spot`
  - `futures`
  - `spot+futures` (по умолчанию).
- Обновляем Telegram UI (полностью на русском):
  - два режима сигналов в интерфейсе;
  - по умолчанию включены оба режима;
  - отдельные переключатели включения/выключения каждого режима.
- Обновляем обработку stale-данных:
  - `stale_ms` не блокирует отправку;
  - если стратегия пропускает сигнал, он отправляется всегда;
  - `stale_ms` используется как внутренняя диагностическая метрика свежести.
- Расширяем retention/очистку:
  - очищаем не только `signals`, но и `analytics_logs`, `news_and_sentiment`, `raw_candidates`, `scan_logs`.
- Масштабируем воркеры:
  - добавляем параметризуемый запуск `1..N` воркеров через env, чтобы увеличить оперативность обработки.
- Включаем расширенную диагностику "сигналы не отправляются":
  - полный debug-режим на 24 часа (raw candidates + reject reasons + ключевые latency этапов).

**Что ожидаем получить:**
- Цена в сигнале становится операционно прозрачной, без блокировок из-за stale.
- Частота сигналов выше за счет калибровки фильтров и параллелизма.
- Гибкая работа по рынкам (`spot/futures`) без хардкода в коде.
- Понятный для пользователя Telegram UI с двумя режимами сигналов.
- Стабильная очистка данных и снижение риска деградации из-за роста БД/памяти.
- Быстрая диагностика причин, если поток сигналов снова остановится.

**Целевая логика реализации:**
1) `Market type` и режимы сигналов хранятся в user settings (глобально на chat_id).
2) Воркер сканирует инструменты согласно market type, формирует:
   - кандидаты ленты pump/dump;
   - кандидаты стратегии pin bar.
3) Любой валидный кандидат проходит dedup/cooldown и отправляется (без stale-блокировки).
4) Все кандидаты и причины reject пишутся в `raw_candidates` и `scan_logs` для диагностики.
5) Retention-процедура по расписанию очищает все шумовые таблицы единым сроком хранения.
6) После локальной проверки: git commit -> push -> деплой на сервер.

---
## [2026-03-10 22:58:00] Реализовано: recommendation + signal side mode + упрощенный UI
**Что сделано:**
- Добавлен helper `app/services/signal_presentation.py`:
  - `build_recommendation(...)` -> `Лонг | Шорт | Наблюдать`;
  - `matches_signal_side_mode(...)` + нормализация режима `all/pump/dump`.
- В API добавлено поле `recommendation`:
  - `SignalOut`, `LiveSignalOut`, `FeedMoverOut`;
  - ответы `/signals`, `/signals/live`, `/feed/movers`, `POST /signals`.
- Добавлен `signal_side_mode` в пользовательские настройки:
  - модель `user_signal_settings`,
  - additive update в `app/db/init.py`,
  - обновлены схемы и API `GET/POST /user-settings`.
- Пользовательский фильтр `min_price_move_pct` унифицирован:
  - дефолт в effective settings -> `5.0`,
  - фильтрация применяется в RSI-воркере и в `/feed/movers` (при `chat_id`).
- В Telegram UI:
  - `Триггер цены` заменен на `Минимальное движение, %` (одно число),
  - добавлен экран `Направление` (`Все / Только pump / Только dump`),
  - экран ленты с нижним меню:
    - `[ Pump ] [ Dump ]`
    - `[ ⬅️ Главное меню ]`
  - правило нижней кнопки `⬅️ Главное меню` приведено к единому виду на всех основных экранах.
- Карточка сигнала обновлена до компактного формата без лишних кнопок под сообщением:
  - symbol, движение, цена, RSI, объём 24ч, рекомендация.

**Тесты:**
- Добавлен `tests/test_signal_presentation.py`.
- Обновлен `tests/test_rsi_engine.py` (кейсы 4.9/5.0/7.0 для порогов 5 и 10).
- `ruff check .` -> `All checks passed`.
- `pytest -q` -> `16 passed`.

---
## [2026-03-10 22:35:00] Планируемые правки: recommendation + упрощенный UI сигналов + side mode
**Что планируем менять перед реализацией:**
- Добавить единый presentation-helper:
  - `build_recommendation(...)` -> `Лонг | Шорт | Наблюдать`;
  - `matches_signal_side_mode(...)` -> фильтр `all/pump/dump`.
- Расширить API и выдачу сигналов полем `recommendation`:
  - `/signals`, `/signals/live`, `/feed/movers`.
- Упростить пользовательскую настройку движения:
  - оставить только `min_price_move_pct` как единый порог;
  - убрать ввод `5m 15m` в Telegram UI.
- Добавить режим направления `signal_side_mode`:
  - хранение в `user_signal_settings`;
  - применение фильтра в RSI-воркере и `/feed/movers` (при `chat_id`).
- Обновить Telegram UI:
  - карточка сигнала в формате:
    - `BTC/USDT`
    - `Движение: +11.2%`
    - `Цена: 61 250`
    - `RSI: 71.4`
    - `Объём 24ч: $12.5M`
    - `Рекомендация: Лонг`
  - без лишних inline-кнопок под каждым сообщением сигнала;
  - нижнее меню только общее (`Pump`, `Dump`, `⬅️ Главное меню`) на экранах интерфейса.

**Принцип реализации:**
- Не менять ключевую торговую логику, только presentation/UI и пользовательские фильтры.
- Сохранить backward compatibility (не удалять старые поля/API без необходимости).

---
## [2026-03-09 14:05:00] Оптимизация цикла: modulo-sharding + общий anti-spam + меньше HTTP-запросов
**Что сделано:**
- Воркер переведен на modulo-шардирование:
  - `WORKER_SHARD_COUNT`
  - `WORKER_SHARD_INDEX`
  - разбиение по формуле `index % count`.
- Добавлен запуск 3 воркеров в `docker-compose`:
  - `signal_worker_1` (`index=0`)
  - `signal_worker_2` (`index=1`)
  - `signal_worker_3` (`index=2`)
- Anti-spam (`cooldown`) переведен в общий Redis-state, чтобы работать глобально между воркерами.
- Убран дублирующий запрос свечей на символ:
  - RSI теперь считается из свечей, уже полученных в `build_snapshot`.
- 24h объемы переведены на общий батч-запрос:
  - один вызов `/ticker/24hr` на шард,
  - далее используется локальная карта `symbol -> quoteVolume`.

**Зачем:**
- Сократить время полного прохода universe.
- Убрать повторные вычисления на той же монете.
- Синхронизировать anti-spam между параллельными воркерами.

**Файлы:**
- `app/services/binance_candles.py`
- `app/services/signal_filters.py`
- `app/workers/mock_signal_worker.py`
- `app/core/config.py`
- `docker-compose.yml`
- `scripts/deploy_server.py`
- `tests/test_signal_filters.py`
- `tests/test_worker_sharding.py`

**Проверки:**
- `ruff check .` -> `All checks passed`.
- `pytest -q` -> `8 passed`.

---
## [2026-03-09 13:20:00] Реализовано: минимальная логика сигналов (price -> RSI -> anti-spam)
**Что изменено:**
- Убрали `volume` из блокирующих условий сигнала.
- Основной триггер оставили только по цене:
  - `abs(price_change_5m) >= 2.5%`
  - `OR abs(price_change_15m) >= 4.5%`
- RSI смягчен:
  - `pump`: `RSI >= 60`
  - `dump`: `RSI <= 40`
- Убрали блокировку по `duplicate`.
- Добавили повторный сигнал при продолжении движения:
  - если цена ушла дальше минимум на `1.5%` от последнего сигнала, новый сигнал разрешен даже в cooldown.

**Упрощенный pipeline:**
- `price_trigger -> rsi_filter -> anti_spam(cooldown + followup_move) -> signal`

**Технически по коду:**
- `app/services/rsi_engine.py`: удален volume-check из `validate_candidate_filters`.
- `app/services/signal_filters.py`: duplicate больше не отклоняет сигнал; добавлен `followup_move_pct`.
- `app/workers/mock_signal_worker.py`: убраны `min_quote_volume` и `weak_context` этапы из блокирующей логики.
- `app/api/router.py`: в `feed/movers` убран `min_quote_volume` фильтр.
- `app/core/config.py`: дефолты RSI изменены на `40/60`, добавлен `signal_followup_move_pct=1.5`.

**Проверки:**
- `ruff check .` -> `All checks passed`.
- `pytest -q` -> `7 passed`.

---
## [2026-03-09 12:45:00] Реализовано: price-trigger pipeline + soft context + мягкие volume пороги
**Что реализовано в коде:**
- Перестроен pipeline сигнала в RSI режиме:
  1) `price trigger` по окнам `5m/15m`
  2) `dynamic volume filter`
  3) `RSI` как фильтр по направлению
  4) `cooldown/duplicate`
- В snapshot добавлены новые поля:
  - `price_change_5m`
  - `price_change_15m`
- Пороговые значения по умолчанию:
  - `SIGNAL_PRICE_CHANGE_5M_TRIGGER_PCT=2.5`
  - `SIGNAL_PRICE_CHANGE_15M_TRIGGER_PCT=4.5`
  - `SIGNAL_VOLUME_MULTIPLIER_BASE=1.35`
  - `SIGNAL_VOLUME_MULTIPLIER_STRONG=1.2`
  - `SIGNAL_STRONG_MOVE_PCT=5.0`
- `context` переведен в soft filter:
  - при слабом контексте нет блокировки сигнала;
  - ставится `weak_context`, снижаются `strength` и `action` до `watch`.

**Логика фильтров (детали):**
- Price trigger: `abs(price_change_5m) >= 2.5 OR abs(price_change_15m) >= 4.5`.
- Dynamic volume:
  - при сильном импульсе (`>= 5.0%`) используется `1.2 * avg20`;
  - иначе `1.35 * avg20`.
- RSI filter:
  - для `pump`: `rsi >= upper`;
  - для `dump`: `rsi <= lower`.

**Reject и служебные статусы:**
- Новые причины в логах:
  - `reject_price_trigger`
  - `reject_volume_dynamic`
  - `reject_rsi_filter`
- Soft status:
  - `weak_context` (без hard reject).

**Обновленные файлы:**
- `app/services/binance_candles.py`
- `app/services/rsi_engine.py`
- `app/workers/mock_signal_worker.py`
- `app/services/feed_formatter.py`
- `app/api/router.py`
- `app/core/config.py`
- `scripts/deploy_server.py`
- `tests/test_rsi_engine.py`

**Проверки:**
- `ruff check .` -> `All checks passed`.
- `pytest -q` -> `5 passed`.

---
## [2026-03-09 12:20:00] Перед реализацией: уточнение параметров новой логики (по комментариям)
**Что фиксируем перед внедрением:**
- Переход на структуру `price trigger -> volume filter -> RSI filter` подтвержден.
- Детекция движения по окнам времени остается:
  - `price_change_5m_trigger_pct = 2.5`
  - `price_change_15m_trigger_pct = 4.5`

**Корректировки порогов (смягчение):**
- `volume_multiplier_strong = 1.2` оставить без изменений.
- `volume_multiplier_base` смягчить с `1.5` до `1.35` (рабочий диапазон обсуждался `1.3-1.4`).
- `strong_move_pct` смягчить с `6.0` до `5.0` (рабочий диапазон обсуждался `4.5-5.0`).

**Важное продуктовое решение по контексту:**
- `context` не должен быть жестким стоп-фильтром на этом этапе.
- Вместо `reject_context` используем мягкий режим:
  - либо метка `weak_context`,
  - либо понижение приоритета сигнала.
- Цель текущего этапа: построить базовую рабочую логику, затем докручивать фильтры итеративно.

**Примечание:**
- Журнал продолжен (без перезаписи файла с нуля), запись добавлена как pre-implementation checkpoint.

---
## [2026-03-09 11:44:00] Реализовано: формула RSI+Price+Volume + настройки в боте
**Что реализовано в коде:**
- Внедрена формула сигнала:
  - `PUMP`: `RSI >= upper`, `pct_change >= +1.5%`, `current_volume >= 2 * avg_volume(20)`.
  - `DUMP`: `RSI <= lower`, `pct_change <= -1.5%`, `current_volume >= 2 * avg_volume(20)`.
- Порядок фильтров в воркере:
  1) RSI trigger
  2) min price move
  3) direction check
  4) volume spike
  5) cooldown
  6) duplicate
- Добавлены reject причины в логах:
  - `reject_rsi`
  - `reject_min_move`
  - `reject_direction`
  - `reject_volume`
  - `reject_cooldown`
  - `reject_duplicate`

**Настройки внутри бота (UI):**
- В экране `Настройки` добавлены пользовательские настройки:
  - `Процент отклонения`
  - `RSI` (нижний/верхний)
  - выбор таймфрейма.
- UI остается простым, без технических объяснений.

**Конфиг добавлен:**
- `SIGNAL_MIN_ABS_CHANGE_PCT=1.5`
- `SIGNAL_VOLUME_SPIKE_MULTIPLIER=2.0`
- `SIGNAL_VOLUME_AVG_WINDOW=20`

**Технические изменения:**
- Расширены данные свечей (текущий объем + средний объем окна).
- Расширены пользовательские настройки (`min_price_move_pct`) в API/БД.
- Обновлен deploy script: фиксирует `SIGNAL_ENGINE_MODE=rsi` и новые signal-флаги в `.env`.

**Проверки:**
- `ruff check .` -> `All checks passed`.
- `pytest` -> `4 passed`.

**Git и деплой:**
- Изменения запушены в `main`: commit `1cd7ba2`.
- Деплой выполнен успешно: `DEPLOY_OK`.

---
## [2026-03-08 18:05:00] Планируемые правки: RSI+Price+Volume и настройки в боте
**Что внедряем:**
- Модель сигнала: `RSI + Price + Volume`.
- Формула:
  - `PUMP`: `RSI >= upper`, `pct_change >= +1.5%`, `current_volume >= 2 * avg_volume(20)`.
  - `DUMP`: `RSI <= lower`, `pct_change <= -1.5%`, `current_volume >= 2 * avg_volume(20)`.
- Порядок фильтров:
  1) RSI trigger
  2) min price move
  3) direction check
  4) volume spike
  5) cooldown
  6) duplicate

**Что добавляем:**
- Reject причины в логах:
  - `reject_rsi`
  - `reject_min_move`
  - `reject_direction`
  - `reject_volume`
  - `reject_cooldown`
  - `reject_duplicate`
- Конфиг:
  - `SIGNAL_MIN_ABS_CHANGE_PCT=1.5`
  - `SIGNAL_VOLUME_SPIKE_MULTIPLIER=2.0`
  - `SIGNAL_VOLUME_AVG_WINDOW=20`
- Настройки в боте (простой UI, без тех. объяснений):
  - `Процент отклонения`
  - `RSI`
  - таймфрейм выбора сигнала.

**UI правило:**
- Не отправлять и не показывать:
  - `PUMP -%`
  - `DUMP +%`
  - любые сигналы с движением `<1.5%`.

---
## [2026-03-08 17:41:00] Лента в этот же чат + единый таймфрейм по умолчанию
**Проблема:**
- Пользовательский сценарий требовал поток сигналов в тот же диалог с ботом (без отдельного канала/chat_id).
- В настройках нужен выбор одного таймфрейма, дефолт `15m`.

**Что изменено:**
- `/start` теперь регистрирует текущий `chat_id` в пользовательских настройках для push-доставки.
- Добавлен API endpoint `GET /user-settings/chats` для списка чатов, в которые слать поток.
- `signal_worker` в режиме RSI рассылает сигналы по зарегистрированным чатам, с per-chat cooldown scope.
- `menu:feed` сообщает, что поток идет в текущий чат.
- Настройки переведены в single-timeframe режим: выбор одного ТФ (`5m/15m/1h/4h`), без мультивыбора.
- Дефолтный ТФ изменен на `15m`.

**Файлы:**
- `app/bot/handlers.py`
- `app/core/config.py`
- `app/services/user_settings.py`
- `app/services/signal_filters.py`
- `app/api/router.py`
- `app/workers/mock_signal_worker.py`

**Проверки:**
- `ruff check ...` -> `All checks passed`.
- `python -m pytest -q` -> `3 passed`.

---
## [2026-03-08 17:24:30] Деплой UX push-only правок
**Что сделано:**
- Локальные UX-правки синхронизированы в git-клон и запушены в `origin/main`.
- Коммит: `e5f996e` (`Simplify bot UX for push-only signal feed`).
- Выполнен повторный деплой через `scripts/deploy_server.py`.

**Результат:**
- Деплой завершен успешно: `DEPLOY_OK`.
- Контейнеры `api`, `bot`, `signal_worker`, `postgres`, `redis` в статусе `Up`.

---
## [2026-03-08 16:25:00] UX правки: push-only лента и чистые настройки
**Что изменено:**
- Упрощен экран `/start`: оставлен короткий пользовательский текст без технических деталей.
- Кнопка `Лента` переведена в push-only поведение:
  - больше не рендерит список сигналов в окне;
  - показывает статус, что сигналы приходят потоком отдельными сообщениями.
- Экран `Настройки` очищен от системной информации:
  - оставлен только выбор активных таймфреймов;
  - убраны кнопки и вывод RSI/min volume.
- Раздел `Информация` упрощен до пользовательского описания без внутренней технической терминологии.

**Файлы:**
- `app/bot/handlers.py`
- `app/bot/keyboards.py`

**Проверки:**
- `ruff check .` -> `All checks passed`.
- `python -m pytest -q` -> `3 passed`.

---
## [2026-03-08 13:05:00] Деплой на сервер выполнен
**Запуск:**
- Выполнен `scripts/deploy_server.py` с локальной машины по SSH.
- Предварительно установлена зависимость `paramiko` (иначе скрипт не стартовал).

**Результат:**
- Статус деплоя: `DEPLOY_OK`.
- На сервере подняты контейнеры:
  - `api` (порт `8000`)
  - `bot`
  - `signal_worker`
  - `postgres` (порт `5432`, healthy)
  - `redis` (порт `6379`, healthy)

**Примечание:**
- Скрипт деплоя подтягивает код из `repo-url` (`git pull` на сервере), а не копирует локальные файлы напрямую.

---
## [2026-03-06 12:58:00] Реализация RSI MVP (Stage 1-7) + автопроверка
**Что реализовано:**
- Stage 1: добавлен feature flag `SIGNAL_ENGINE_MODE` и конфиги RSI/Universe/anti-dup в `app/core/config.py`.
- Stage 1: создан новый отдельный сервисный каркас:
  - `app/services/binance_universe.py`
  - `app/services/binance_candles.py`
  - `app/services/rsi_engine.py`
  - `app/services/signal_filters.py`
  - `app/services/feed_formatter.py`
  - `app/services/user_settings.py`
- Stage 2-3: реализован полный Binance spot USDT universe и RSI сигналы (`>= upper -> pump`, `<= lower -> dump`) для `5m/15m/1h/4h`.
- Stage 4: внедрен anti-spam:
  - cooldown key = `(symbol, timeframe, signal_type)`;
  - anti-duplicate fingerprint window.
- Stage 5: расширены модели/схемы/API:
  - `signals`: `signal_type`, `trigger_source`, `rsi_value`, `prev_price`;
  - добавлена `user_signal_settings` для `global defaults + optional per-chat override`;
  - добавлены `/user-settings` (GET/POST).
- Stage 6: обновлен UI под MVP:
  - главное меню: `Лента / Настройки / Информация`;
  - карточка сигнала в формате MVP (без RSI в тексте, без сложных метрик);
  - настройки: таймфреймы + RSI lower/upper + reset.
- Stage 7: добавлено логирование reject-причин (`threshold`, `cooldown`, `duplicate`, `bad_data`) в воркере.
- Добавлен smoke-тест `tests/test_rsi_engine.py`.

**Совместимость и rollout:**
- Legacy логика сохранена и работает при `SIGNAL_ENGINE_MODE=legacy`.
- Новый RSI поток активируется переключением `SIGNAL_ENGINE_MODE=rsi`.

**Проверки качества:**
- `ruff check .` -> `All checks passed`.
- `python -m pytest -q` -> `3 passed`.

---
## [2026-03-06 12:10:00] План внедрения RSI MVP (до начала правок)
**Что делаем:**
- Реализуем новый сценарий тестового сигнального бота с feature flag `SIGNAL_ENGINE_MODE = legacy | rsi`.
- Внедряем новую модульную архитектуру без смешивания нового и legacy кода.
- После каждого stage фиксируем checkpoint в отчете.

**Зафиксированные продуктовые решения:**
- Universe: все Binance spot пары с `quote=USDT` и `status=TRADING`.
- RSI правила:
  - `RSI >= upper` => `Pump`
  - `RSI <= lower` => `Dump`
- Таймфреймы: `5m`, `15m`, `1h`, `4h`.
- Настройки: `global defaults + optional per-chat override`.
- Cooldown ключ: `(symbol, timeframe, signal_type)`.
- Daily counter: по `(symbol, signal_type)`.
- Legacy логика сохраняется до полного переключения.

**Новые сервисы (минимальная структура):**
- `app/services/binance_universe.py`
- `app/services/binance_candles.py`
- `app/services/rsi_engine.py`
- `app/services/signal_filters.py`
- `app/services/feed_formatter.py`
- `app/services/user_settings.py`

**Stage порядок:**
1. Stage 1 — feature flag + архитектурный каркас
2. Stage 2 — полный Binance USDT universe
3. Stage 3 — RSI engine + 4 таймфрейма
4. Stage 4 — cooldown + anti-duplicate
5. Stage 5 — обновление БД + настройки
6. Stage 6 — новая карточка + упрощенное меню
7. Stage 7 — логи + smoke tests + безопасный rollout

**Ожидаемый результат:**
- Новый RSI поток сигналов работает изолированно от legacy.
- Переключение режима делается конфигом без переписывания кода.
- UI показывает упрощенную карточку сигнала:
  - эмодзи, монета, тип (PUMP/DUMP), % изменения, цена до/после, время.

---
## [2026-02-18 20:01:00] Уточнение логики AI Engine
**Что делаем:** 
- Уточнена механика работы AI Engine в `CUR`.

**Как работает (уточнение):**
- Бот использует **взвешенную сумму** факторов, а не прямой арбитраж между CEX и DEX.
- `cex_strength` (сила тренда) и `dex_price_change_pct` (изменение цены за час на DEX) обрабатываются независимо.
- Основная задача: подтверждение тренда одного источника данными из другого + учет сентимента новостей.

**Файлы:**
- `app/api/router.py`: здесь собираются данные из CEX и DEX.
- `app/services/ai_engine.py`: здесь происходит расчет итогового скоринга.

**Критерий успеха:**
- В `CUR` зафиксировано, что система работает на основе гибридного скоринга, а не ценового арбитража.

---
## [2026-02-18 19:47:00] Анализ проекта и текущей логики
**Что делаем:** 
- Проведен полный разбор структуры и логики проекта.
- Обновлен файл `CUR` (теперь записи добавляются сверху, язык — русский).

**Описание логики:**
Проект представляет собой комплексную систему для крипто-аналитики и генерации сигналов:
1. **FastAPI Backend (`app/api/`):** Центральный узел. Обрабатывает запросы бота и воркера, управляет БД (PostgreSQL).
2. **Telegram Bot (`app/bot/`):** Интерфейс пользователя на базе `aiogram`. Позволяет настраивать подписки и просматривать аналитику.
3. **Mock Signal Worker (`app/workers/`):** Фоновый процесс. Каждые N секунд проверяет активные подписки, генерирует сигналы через API и рассылает их в Telegram.
4. **AI Engine (`app/services/ai_engine.py`):** "Мозги" системы. Считает скоринг (entry/watch/hold) на основе:
   - CEX strength (вес ~55%)
   - DEX price change (вес ~25%)
   - Sentiment (новости) (вес ~20%)
   Имеет функцию `tune_strategy_from_history` для авто-подстройки порогов входа на основе винрейта и PnL.
5. **Инфраструктура:** Docker-compose (App, Postgres, Redis). БД управляется через Alembic.

**Системные аспекты:**
- Переменные окружения хранятся в `.env` (загружаются через `pydantic-settings`).
- Логирование настроено централизованно через `app/core/logging.py`.
- Взаимодействие компонентов идет через HTTP (воркер -> API, бот -> API).

**Файлы:**
- `CUR`: системный журнал (этот файл).
- `pyproject.toml`: зависимости проекта.
- `app/core/config.py`: глобальные настройки.

**Критерий успеха:**
- Полное понимание структуры проекта для дальнейшей разработки.
- Файл `CUR` корректно отображает текущее состояние и логику на русском языке.

---
## [2026-02-18 19:43:00] Initial Creation
**Feature/Change:** 
- Creation of the `CUR` system instruction and journal.
- Initialization of the project logging structure.

**How & Files:**
- Created `CUR` in the root directory.
- Added `CUR` to `.gitignore` to prevent it from being pushed to git.

**Success Criteria:**
- `CUR` file exists in root.
- `CUR` is listed in `.gitignore`.
- Future updates will follow this structure.
---
